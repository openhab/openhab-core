variables: !sub
  # Test when variables block itself has !sub tag
  first: hello
  second: ${first} world
  third_key: third
  ${third_key}: ${second}!!!
  nosub: !nosub ${first}

data:
  greeting: !sub ${second}
  exclamation: !sub ${third}
  nosub: !sub ${nosub}
