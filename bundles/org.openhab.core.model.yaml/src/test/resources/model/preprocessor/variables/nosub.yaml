variables:
  foo: bar

top: !sub
  level1: !nosub
    level2: ${foo} # => ${foo}
    level2_sub: !sub
      level3: ${foo} # => bar
      level3_nosub: !nosub
        level4: ${foo} # => ${foo}
        level4_sub: !sub
          level5: ${foo} # => bar