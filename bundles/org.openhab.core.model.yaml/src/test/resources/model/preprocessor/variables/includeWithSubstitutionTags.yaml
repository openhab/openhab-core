variables:
  mainmoo: cow
  foo: bar

plain:
  data: !include
    file: includeWithSubstitutionTags.inc.yaml
    vars:
      moo: ${mainmoo}
sub: !sub
  data: !include
    file: includeWithSubstitutionTags.inc.yaml
    vars:
      moo: ${mainmoo} # This gets substituted to 'cow' then passed into the include
nosub: !nosub
  data: !include
    file: includeWithSubstitutionTags.inc.yaml
    vars:
      moo: ${mainmoo}